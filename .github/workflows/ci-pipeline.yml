name: CI Pipeline

on:
  push:
    branches:
      - dev
      - main
    paths:
      - "LINTelligent.Api/src/**"
      - "LINTelligent.Api/tests/**"
  pull_request:
    branches:
      - main
    paths:
      - "LINTelligent.Api/src/**"
      - "LINTelligent.Api/tests/**"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Pull source code
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore and Build
        run: |
          dotnet restore LINTelligent.Api/LINTelligent.slnx
          dotnet build LINTelligent.Api/LINTelligent.slnx --no-restore --configuration Release

      - name: Run Unit Tests
        run: dotnet test LINTelligent.Api/tests/LINTelligent.UnitTests/LINTelligent.UnitTests.csproj --no-build --configuration Release

      - name: Run Integration Tests
        run: dotnet test LINTelligent.Api/tests/LINTelligent.IntegrationTests/LINTelligent.IntegrationTests.csproj --no-build --configuration Release
